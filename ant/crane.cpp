#include <iostream>
#include <vector>
#include <algorithm>
#define rep(i, n) for (int i = 0; i < (n); i++) 
using namespace std;
const int NMAX = 10010;

int L[NMAX];
int main() {
    int N;
    cin >> N;
    rep(i, N) cin >> L[i];
    
}